/* const test = () => {
  const testList = new LinkedList<number>();

  testList.add(1);
  testList.add(2);
  testList.add(3);

  let testArray: number[] = [];
  for (let item of testList) {
    testArray.push(item.value);
  }

  let firstItem = testList.removeFirst();
  firstItem = testList.removeFirst();
  firstItem = testList.removeFirst();
  firstItem = testList.removeFirst();

  testArray = [];
  for (let item of testList) {
    testArray.push(item.value);
  }

  testList.add(-1);
  testList.add(-2);
  testList.add(-3);

  testArray = [];
  for (let item of testList) {
    testArray.push(item.value);
  }

  let lastItem = testList.removeLast();
  lastItem = testList.removeLast();
  lastItem = testList.removeLast();
  lastItem = testList.removeLast();

  testArray = [];
  for (let item of testList) {
    testArray.push(item.value);
  }

  debugger;
} */